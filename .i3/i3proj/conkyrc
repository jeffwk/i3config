conky.config = {
    background = false,
    out_to_console = true,
    max_text_width = 0,
    update_interval = 1.0,
    total_run_times = 0,
    use_spacer = 'none',
    short_units = true,
    cpu_avg_samples = 2,
};

conky.text = 
[[{"cpu": "${cpu cpu0}", "mem": ["${mem}", "${memmax}"], "diskio": ["${diskio_read}", "${diskio_write}"], "netspeed": ["${downspeedf br0}K", "${upspeedf br0}K"], "fsusage": ["${fs_used}", "${fs_size}"], "date": "${time %a %b %d}", "time": "${time %_I:%M %p}"}]];
